#!/bin/bash

echo -e "\nStarting VIM config setup...\n"

DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
cd $DIR

echo "Nothing to set up for vim"

echo -e "\nVIM config setup complete!\n"